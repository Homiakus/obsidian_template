{
  "name": "api-toolbar",
  "version": "0.1.0",
  "description": "Obsidian plugin providing bottom toolbar buttons that call external HTTP APIs.",
  "main": "main.js",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --external:obsidian --format=cjs --outfile=main.js",
    "dev": "npm run build -- --watch",
    "test": "echo \"No tests\""
  },
  "devDependencies": {
    "@types/node": "^20.11.19",
    "esbuild": "^0.19.0",
    "obsidian": "^1.5.0",
    "typescript": "^5.4.0",
    "zod": "^3.22.0"
  }
}
